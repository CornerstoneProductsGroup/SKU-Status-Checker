# Retailer Listing Monitor (Depot - Free)

## Run
pip install -r requirements.txt
streamlit run app.py

## Input
Upload your Excel file with a sheet named **Depot** containing columns:
OMSID, Internet #, SKU #, UPC, GTIN, Vendor
